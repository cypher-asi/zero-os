[package]
name = "orbital-init"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Init process (PID 1) for Orbital OS - service bootstrap and registry"

[lib]
crate-type = ["cdylib"]

[features]
default = []

[dependencies]
orbital-process.workspace = true

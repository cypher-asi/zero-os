[package]
name = "orbital-hal"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Hardware Abstraction Layer trait for Orbital OS"

[features]
default = []
std = []

[dependencies]
# No external dependencies - uses only core and alloc

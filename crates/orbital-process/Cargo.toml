[package]
name = "orbital-process"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Process-side syscall library for Orbital OS"

[features]
default = []

[dependencies]

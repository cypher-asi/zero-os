// Unified auth panel (recommended)
export { AuthPanel, type AuthView } from './AuthPanel';

// Content components (for embedding in AuthPanel)
export { LoginContent } from './LoginModal';
export { RegisterContent } from './RegisterWizard';

// Shared utilities
export { getProviderIcon, capitalize } from './shared/icons';
export { EthereumIcon, GoogleIcon, XIcon, SolanaIcon } from './shared/icons';

// AuthLink component
export { AuthLink } from './AuthLink';
